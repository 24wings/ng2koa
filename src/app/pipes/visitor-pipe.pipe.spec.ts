import { VisitorPipePipe } from './visitor-pipe.pipe';

describe('VisitorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new VisitorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
